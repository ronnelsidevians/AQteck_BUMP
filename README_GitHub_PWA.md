# BUMP Search PWA v12 MODULE FIXED

Виправлено визначення модулів і відображення каналів:
- додано поля `Основний модуль` та `Модулі/канали` у картку;
- для потенціометрів тунельних клапанів основний модуль береться з AI-каналу, а AO6A/AO6B лишається як живлення/загальний;
- `+1В` для AO6A змінено на `Вихід 10В`;
- DO10 {A7} виділено в окрему систему `Шнек заповнення кормового бункера 4 — DO10 {A7}`, щоб не зливався з DO6;
- збережено центрування схем з v11.

Замінити у GitHub: `index.html`, `sw.js`, `manifest.webmanifest`, `icons/`, `favicon.ico`. Потім очистити Service Worker або відкрити `?v=12`.
